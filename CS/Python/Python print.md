`print()` has an argument `end` with default value `\n`. It adds `end` to the end of the string.
To print something without going to a new line, change set `end=''`

`print("*" * 20)` prints "\*" 20 times
